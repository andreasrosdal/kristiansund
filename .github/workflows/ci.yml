name: continuous integration

on:
  - pull_request
  - push
  - workflow_dispatch

jobs:
  normal:
    runs-on: ubuntu-22.04
    steps:
      - name: check out app
        uses: actions/checkout@v3.5.2
      - name: prepare
        run: |

      - name: install
        shell: bash
        run: mvn compile
