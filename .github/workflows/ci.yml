name: continuous integration

on:
  - pull_request
  - push
  - workflow_dispatch

jobs:
  normal:
    runs-on: ubuntu-latest
    steps:
      - name: check out app
        uses: actions/checkout@v4.1.1
      - name: prepare
        run: |

      - name: install
        shell: bash
        run: mvn compile
